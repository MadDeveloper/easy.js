export default {
    /*
     * Expose protected routes, reachable if user is authenticated
     * Each protected routes will be used as RegExp matching with request url
     * So, for example, you can write:
     *   '/roles'
     */
	protected: []
}
